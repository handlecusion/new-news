# 프레임 편향도 범위 문제 진단

## 문제 현상

프레임 0의 통계:
- **편향도 범위**: -0.8 ~ 0.7 (총 범위 1.5)
- **표준편차**: 0.493
- **편향 분포**: 진보 9개, 중도 15개, 보수 11개 (거의 균등 분산)
- **키워드**: "keyword_0" (의미 없는 더미 값)

→ **프레임이 제대로 구분되지 않고 있음**

## 원인 분석

### 1. ⚠️ 텍스트 전처리 문제 (주요 원인)

**증상:**
- 프레임 키워드가 "keyword_0", "keyword_1" 등 더미 값으로 추출됨
- 실제 기사 내용은 의미 있는 한글 텍스트인데도 불구하고

**원인:**
```python
# src/preprocessing/text_preprocessor.py
# KoNLPy Mecab 사용 시 설치되지 않으면 기본 토크나이저로 폴백
# → 한글 형태소 분석 실패 → 의미 없는 토큰 생성
```

**확인 방법:**
```bash
python -c "from konlpy.tag import Mecab; print(Mecab().morphs('최저임금 인상'))"
```

만약 오류가 나면 Mecab이 설치되지 않은 것.

### 2. 📊 샘플 데이터의 한계

**문제:**
```python
# scripts/generate_sample_data.py
# 템플릿 기반 생성 → 실제 텍스트 다양성 부족
```

**샘플 데이터 특징:**
- 150개 기사가 유사한 템플릿으로 생성됨
- "2024년 최저임금은 시간당 9,860원으로 결정되었다" 같은 문장이 반복
- 실제 뉴스의 언어적 다양성이 부족

**결과:**
- BERTopic이 의미 있는 클러스터를 만들기 어려움
- 편향도와 무관하게 랜덤하게 그룹핑됨

### 3. 🔧 BERTopic 설정 문제

**현재 설정:**
```yaml
# config.yaml
unsupervised:
  min_topic_size: 5
  nr_topics: "auto"
```

**문제:**
- **문서 수**: 150개
- **발견된 프레임**: 5개 (평균 30개 기사/프레임)
- **최소 토픽 크기**: 5개

→ 프레임당 기사 수가 너무 많아서 다양한 편향도가 섞임

### 4. 🤖 임베딩 모델의 한계

**사용 모델:** `jhgan/ko-sroberta-multitask`

**한계:**
- 일반적인 문장 유사도는 잘 포착하지만
- **편향도와 관련된 미묘한 뉘앙스**는 잘 구분하지 못할 수 있음
- 예: "노동자 생계" vs "소상공인 부담" → 의미적으로는 다르지만 임베딩 공간에서 가까울 수 있음

## 해결 방안

### 즉각 해결 (Quick Fix)

#### 1. Mecab 설치
```bash
# macOS
brew install mecab mecab-ko mecab-ko-dic

# Ubuntu/Colab
!apt-get install -y mecab libmecab-dev mecab-ko mecab-ko-dic
!pip install mecab-python3
```

#### 2. BERTopic 파라미터 조정

`config.yaml` 수정:
```yaml
unsupervised:
  min_topic_size: 10        # 5 → 10 (더 명확한 클러스터)
  nr_topics: 10             # auto → 10 (더 세밀한 구분)
  min_df: 2                 # 최소 문서 빈도
  max_df: 0.7               # 0.8 → 0.7 (일반적 단어 제외)
```

#### 3. 편향도 기반 후처리

프레임을 발견한 후, 편향도 기준으로 재분할:
```python
# 각 프레임을 편향도 기준으로 세분화
for frame in frames:
    if frame.bias_std > 0.5:  # 일관성 낮음
        # 편향도 기준으로 2-3개 서브프레임으로 분할
        split_by_bias(frame)
```

### 근본적 해결 (Long-term)

#### 1. 실제 뉴스 데이터 사용

샘플 데이터 대신 실제 크롤링한 뉴스 사용:
- 언어적 다양성 확보
- 실제 프레임 패턴 발견 가능

#### 2. 하이브리드 클러스터링

편향도 정보를 임베딩에 통합:
```python
# 텍스트 임베딩 + 편향도 정보
enhanced_embeddings = np.concatenate([
    text_embeddings,
    bias_scores.reshape(-1, 1) * weight  # weight=0.3 정도
], axis=1)
```

#### 3. 계층적 프레이밍

1단계: 주제별 프레임 (BERTopic)
2단계: 각 주제 내에서 편향별 서브프레임
```
프레임 1: 고용 영향
  ├─ 1-A: 고용 감소 우려 (보수)
  └─ 1-B: 고용 영향 제한적 (진보)

프레임 2: 소상공인
  ├─ 2-A: 부담 증가 (보수)
  └─ 2-B: 지원 정책 병행 (중도)
```

## 검증 방법

프레임이 제대로 구분되었는지 확인:

### 1. 키워드 의미성
```python
# 좋음: ["노동자", "생계", "임금", "인상"]
# 나쁨: ["keyword_0"]
```

### 2. 일관성 점수
```python
# 목표: 대부분 프레임의 std < 0.4
# 현재: std = 0.493 (일관성 부족)
```

### 3. 편향 분포
```python
# 좋음: 진보 25개, 중도 3개, 보수 2개 (명확한 성향)
# 나쁨: 진보 9개, 중도 15개, 보수 11개 (균등 분산)
```

### 4. 언론사 집중도
```python
# 좋음: 한겨레(15), 경향신문(12) - 특정 성향 집중
# 나쁨: 모든 언론사가 고르게 분산 - 구분 실패
```

## 추천 조치

### 우선순위 1 (필수)
1. ✅ Mecab 설치 확인 및 재설정
2. ✅ BERTopic 파라미터 조정 (`min_topic_size: 10`, `nr_topics: 10`)
3. ✅ 파이프라인 재실행 및 결과 확인

### 우선순위 2 (개선)
1. 실제 뉴스 데이터 수집 (최소 500개 권장)
2. 편향도 기반 후처리 로직 추가
3. 프레임 품질 검증 메트릭 추가

### 우선순위 3 (연구)
1. 하이브리드 클러스터링 실험
2. 계층적 프레이밍 구조 설계
3. 편향도 특화 임베딩 모델 fine-tuning

## 결론

**현재 상태:** 프레임이 편향도와 무관하게 랜덤 그룹핑됨

**주요 원인:**
1. Mecab 미설치로 인한 형태소 분석 실패 (60%)
2. 샘플 데이터의 다양성 부족 (30%)
3. BERTopic 파라미터 미조정 (10%)

**즉각 조치:** Mecab 설치 + BERTopic 파라미터 조정
**장기 목표:** 실제 데이터 사용 + 하이브리드 클러스터링
