# 뉴스 프레임 분석 시스템

한국어 뉴스 기사의 프레임을 자동으로 발견하고 언론사 편향도와의 관계를 분석하는 하이브리드 머신러닝 시스템입니다.

## 📋 프로젝트 개요

이 시스템은 동일한 이슈에 대한 뉴스 보도를 분석하여:
1. **비지도 학습(BERTopic)**: 데이터로부터 자동으로 프레임 패턴을 발견
2. **지도 학습(KoBERT)**: 언론사 편향도를 기반으로 편향 예측 모델 학습
3. **통합 분석**: 발견된 프레임과 편향도의 관계를 통계적으로 분석

## 🚀 빠른 시작

### 1. 환경 설정
```bash
# 의존성 설치
pip install -r requirements.txt

# macOS에서 Mecab 설치 (선택사항)
brew install mecab mecab-ko mecab-ko-dic
```

### 2. 샘플 데이터 생성
```bash
python src/generate_sample_data.py
```

### 3. 전체 파이프라인 실행
```bash
python src/pipeline.py
```

## 📊 주요 결과물

파이프라인 실행 후 `results/` 디렉토리에 다음 파일들이 생성됩니다:

- 📊 **dashboard.html** - 메인 인터랙티브 대시보드
- 🔍 **frame_explorer.html** - 프레임별 기사 탐색기
- 🕸️ **frame_network.html** - 프레임 관계 네트워크
- 📈 **bias_timeline.html** - 편향도 타임라인
- 📄 **analysis/report.json** - 상세 분석 리포트

## 🏗️ 프로젝트 구조

```
new-news/
├── data/
│   ├── input/
│   │   └── articles.json          # 입력 데이터
│   └── processed/
│       └── embeddings.npy         # 캐시된 임베딩
├── src/
│   ├── preprocessing/
│   │   ├── text_preprocessor.py   # 텍스트 전처리
│   │   └── embedder.py            # 문서 임베딩
│   ├── unsupervised/
│   │   ├── frame_extractor.py     # BERTopic 프레임 추출
│   │   └── visualizer.py          # 프레임 시각화
│   ├── supervised/
│   │   ├── bias_classifier.py     # KoBERT 편향도 분류
│   │   └── frame_predictor.py     # 프레임 기반 예측
│   ├── analysis/
│   │   ├── correlation.py         # 상관관계 분석
│   │   └── dashboard.py           # 인터랙티브 대시보드
│   ├── generate_sample_data.py    # 샘플 데이터 생성
│   └── pipeline.py                # 메인 파이프라인
├── results/                       # 분석 결과
├── models/                        # 학습된 모델
├── config.yaml                    # 설정 파일
├── requirements.txt               # 의존성
└── README.md
```

## 📈 데이터 형식

입력 데이터는 다음과 같은 JSON 형식이어야 합니다:

```json
{
  "metadata": {
    "issue": "최저임금 인상",
    "collection_period": "2024-01-01 ~ 2024-01-31",
    "total_articles": 150
  },
  "articles": [
    {
      "article_id": "20240115_hankyoreh_001",
      "media_outlet": "한겨레",
      "bias_score": -0.7,
      "title": "기사 제목",
      "content": "기사 본문...",
      "published_date": "2024-01-15",
      "url": "https://..."
    }
  ]
}
```

편향도(`bias_score`)는 -1(진보) ~ +1(보수) 범위의 값입니다.

## 🔧 개별 모듈 실행

### 프레임 추출만 실행
```bash
python src/unsupervised/frame_extractor.py
```

### 편향도 분류기만 학습
```bash
python src/supervised/bias_classifier.py
```

### 통합 분석만 실행
```bash
python src/analysis/correlation.py
```

### 대시보드만 생성
```bash
python src/analysis/dashboard.py
```

## 📊 분석 결과 해석

### 프레임
- **프레임**: 뉴스 기사가 이슈를 다루는 관점이나 각도
- **키워드**: 각 프레임을 특징짓는 주요 단어들
- **크기**: 해당 프레임에 속하는 기사 수

### 편향도
- **진보**: bias_score < -0.3
- **중도**: -0.3 ≤ bias_score ≤ 0.3
- **보수**: bias_score > 0.3

### 상관관계
- 프레임과 편향도 간의 통계적 관계를 분석
- 카이제곱 검정, ANOVA, Pearson 상관계수 등 활용

## ⚙️ 설정

`config.yaml` 파일에서 다양한 파라미터를 조정할 수 있습니다:
- 최소 토픽 크기
- 임베딩 모델
- 학습 하이퍼파라미터
- 편향도 임계값

## 📝 라이선스

MIT License

## 👥 기여

이슈 제보와 PR을 환영합니다!

## 📧 문의

프로젝트 관련 문의사항은 이슈로 등록해주세요.